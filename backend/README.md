# Backend placeholder

Not implemented. The proposed backend covers account identity, server-owned hero fragments, once-only run rewards, transactional free unlocks and validated Google Play entitlements. See `../docs/architecture.md` and `../docs/data-contracts.md`. Choose runtime, package/toolchain and hosting configuration through an approved BMAD architecture decision before generating actual code. Never commit service-account JSON, secrets, production keys or unredacted purchase tokens.
